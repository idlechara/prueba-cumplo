<%= form_for(transferral) do |f| %>
  <% if transferral.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transferral.errors.count, "error") %> prohibited this transferral from being saved:</h2>

      <ul>
      <% transferral.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
